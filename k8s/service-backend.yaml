apiVersion: v1
kind: Service
metadata:
  name: priority-backend
  namespace: temporal-priority-demo
  labels:
    app: priority-backend
    component: backend
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 7080
    targetPort: 7080
    protocol: TCP
  selector:
    app: priority-backend

